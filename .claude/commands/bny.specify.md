Run `bny specify $ARGUMENTS` and report the result.

If no arguments are provided, ask the user what feature to specify.

Parse the JSON output. On success, tell the user the branch name, spec file path, and issue number. On error, show the error message.
