Run `bny review $ARGUMENTS` and report the result.

This runs a Gemini antagonist review of the current feature's spec/plan/tasks. Use `--prompt-only` to write the review prompt to a file instead of running Gemini.

Show the review output to the user. If it fails, show the error.
