Run `bny brane tldr $ARGUMENTS` and report the result.

This shows an instant outline of the brane worldview with TL;DR synopses. Zero LLM calls — reads file headers directly.

Common flags:
- `--json` — machine-readable output

Show the outline to the user.
