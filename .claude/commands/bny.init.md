Run `bny init $ARGUMENTS` and report the result.

Scaffolds a new project for bny: creates `.bny/`, `dev/`, `.githooks/` with lean defaults. Idempotent — skips existing files.

Common flags:
- `--minimal` — just `.bny/` state directory

Show the result to the user.
