Run `bny brane ask $ARGUMENTS` and report the result.

This asks the project brane a question using its accumulated worldview as context. The answer includes source provenance.

Show the answer to the user.
