Run `bny tasks $ARGUMENTS` and report the result.

If no feature name is given and we're not on a feature branch, tell the user to specify one or switch branches.

Parse the JSON output. On success, tell the user the tasks file path. On error, show the error message.
