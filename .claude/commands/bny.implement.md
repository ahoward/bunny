Run `bny implement $ARGUMENTS` and report the result.

This shells out to `claude -p` with a prompt built from the current feature's spec/plan/tasks. It may take a while.

Show the user the output when complete. If it fails, show the error.
