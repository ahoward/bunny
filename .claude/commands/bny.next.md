Run `bny next $ARGUMENTS` and report the result.

This picks the next roadmap item and runs the full pipeline: pre_flight, specify, plan, tasks, review, implement, post_flight.

Show progress and final output. If it fails at any stage, show which stage failed and the error.
