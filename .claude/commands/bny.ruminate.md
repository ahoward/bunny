Run `bny ruminate $ARGUMENTS` and report the result.

This reflects on the current feature implementation and feeds insights into the brane. It gathers the spec, plan, tasks, and git diff, then extracts durable knowledge (patterns, decisions, gaps) into the worldview.

Show the intake diff preview and final output. If it fails, show the error.
