Run `bny map $ARGUMENTS` and report the result.

This generates a structural map of the codebase using tree-sitter. It parses source files and extracts exported symbols, classes, types, and imports. Supports TypeScript, JavaScript, Ruby, Python, and Go.

Show the map output. If it fails, show the error.
