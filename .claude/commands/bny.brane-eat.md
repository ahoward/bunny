Run `bny brane eat $ARGUMENTS` and report the result.

This feeds a file into the project brane (worldview). It shows a diff preview and asks for confirmation before applying changes.

Common flags:
- `--yes` / `-y` — skip confirmation, apply immediately
- `--dry-run` — show the prompt that would be sent, don't call claude

Show the intake diff and result to the user.
