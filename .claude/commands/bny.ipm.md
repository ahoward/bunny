Run `bny ipm $ARGUMENTS` and report the result.

This launches an Iteration Planning Meeting — an interactive loop where claude reviews the project, asks questions, and produces a roadmap. Use `--dry-run` to see the initial prompt without running.

Show the output to the user. Note: this is interactive and may require human input.
