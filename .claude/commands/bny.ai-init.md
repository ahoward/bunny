Run `bny ai init $ARGUMENTS` and report the result.

Bootstraps AI tool integration by creating symlinks (CLAUDE.md, GEMINI.md, AGENTS.md) pointing to the agent protocol.

Show the result to the user.
