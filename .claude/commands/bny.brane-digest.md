Run `bny brane digest $ARGUMENTS` and report the result.

This clears the worldview and re-eats all tracked sources from scratch. Use `--yes` to skip the confirmation prompt. Use `--dry-run` to see what would be re-eaten.

Show the result to the user.
