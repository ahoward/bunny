{
  "name": "mlint",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "mlint": "./bin/mlint"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build src/cli.ts --outdir dist --target bun"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
