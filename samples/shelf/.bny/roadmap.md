# Roadmap

## Next

- [x] Core bookmark API — POST /bookmarks to save a URL with tags and notes, GET /bookmarks to list all, DELETE /bookmarks/:id to remove
- [x] Search and filtering — GET /bookmarks?tag=X for tag filtering, GET /bookmarks?q=X for full-text search across URLs notes and tags
- [x] Markdown export — GET /export returns all bookmarks as a formatted markdown document grouped by tag

## Done
